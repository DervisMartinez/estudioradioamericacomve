server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html;
    # Esta línea es clave: si no encuentra un archivo o directorio,
    # devuelve el index.html para que React Router maneje la ruta.
    try_files $uri $uri/ /index.html;
  }
}